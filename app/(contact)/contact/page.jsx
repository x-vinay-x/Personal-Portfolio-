import React from 'react';
import ContactContent from './components/ContactContent';

export const metadata = {
    title: "Vinay N - Contact",
    description: "Get in touch with Vinay N. Available for collaborations, projects, or a quick chat.",
};

const ContactPage = () => {
    return (
        <ContactContent />
    );
};

export default ContactPage;